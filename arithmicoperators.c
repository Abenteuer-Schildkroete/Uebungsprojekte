#include <stdio.h>

int main()
{
    //ARITHMIC OPERATORS
    
    int x = 5;
    int y = 10;
    
    //int z = x + y;            // + (addieren)
    //int z = x - y;            // - (subtrahieren)
    //int z = x * y;            // * (multiplizieren)
    //float z = x / (float) y;  // / (dividieren)
    //int z = x % y;            // % (Modulus)
    //int z = x++;            // ++ (plus 1)
    int z = x--;            // -- (minus 1)

    printf("%d", z);
    //printf("%f", z);

    return 0;
}