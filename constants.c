#include <stdio.h>

int main()
{
    //CONSTANTS
    //Konstanten sorgen dafuer das Werte in als konstant definierte Variablen unveränderbar werden und somit durch das Programm nicht mehr überschrieben werden können.

    const float pi = 3.141592;

    printf("%f", pi);

    return 0;
}